set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

## Rebind tmux prefix
# set -g prefix C-a
# unbind C-b
# bind-key C-a send-prefix

## Rebind some keymaps from default
# unbind '%'
# unbind ä
# bind ä split-window -h
#
# unbind '"'
# bind - split-window -v
#
# unbind r
bind r source-file ~/.tmux.conf

## -- Keybindings --
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind h select-pane -L
bind -r m resize-pane -Z
set-option -g status-position top

# Start window and pane from 1 because 0 in at the end of the number row
set -g base-index 1
setw -g pane-base-index 1

## Mouse and delay imporovements
set -g mouse on
set -g status-interval 1

# remove delay for exiting insert mode with ESC in Neovim
set -sg escape-time 10

## Vi-style
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection # start selecting text with "v"
bind-key -T copy-mode-vi 'y' send -X copy-selection # copy text with "y"
unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode when dragging with mouse

## -- TPM plugin declarations --
set -g @plugin 'catppuccin/tmux#2.1.3'
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
# set -g @plugin 'tmux-plugins/tmux-resurrect' # persist tmux sessions after computer restart
# set -g @plugin 'tmux-plugins/tmux-continuum' # automatically saves sessions for you every 15 minutes


## -- Catpuccin theme settings --
set -g @catppuccin_flavor 'macchiato'
set -g @catppuccin_status_background "transparent"
set -g @catppuccin_window_status_style "none"
set -g @catppuccin_pane_status_enabled "off"
set -g @catppuccin_pane_border_status "off"

## -- Status line config --

# Status left
set -g status-left-length 100
set -g status-left ""
set -ga status-left "#{?client_prefix,#{#[bg=#{@thm_red},fg=#{@thm_mantle},bold]  #S },#{#[bg=#{@thm_mantle},fg=#{@thm_green}]  #S }}"
set -ga status-left "#[bg=#{@thm_mantle},fg=#{@thm_overlay_0},none]│"
set -ga status-left "#[bg=#{@thm_mantle},fg=#{@thm_maroon}]  #{pane_current_command} "
set -ga status-left "#[bg=#{@thm_mantle},fg=#{@thm_overlay_0},none]│"
set -ga status-left "#[bg=#{@thm_mantle},fg=#{@thm_blue}]  #{=/-32/...:#{s|$USER|~|:#{b:pane_current_path}}} "

# Status right
set -g status-right-length 100
set -g status-right ""
set -ga status-right "#{?#{e|>=:10,#{battery_percentage}},#{#[bg=#{@thm_red},fg=#{@thm_mantle}]},#{#[bg=#{@thm_mantle},fg=#{@thm_pink}]}} #{battery_icon} #{battery_percentage} "
set -ga status-right "#[bg=#{@thm_mantle}]#{?#{==:#{online_status},ok},#[fg=#{@thm_mauve}] 󰖩 on ,#[fg=#{@thm_red},bold]#[reverse] 󰖪 off }"
set -ga status-right "#[bg=#{@thm_mantle},fg=#{@thm_overlay_0}, none]│"
set -ga status-right "#[bg=#{@thm_mantle},fg=#{@thm_blue}] 󰭦 %Y-%m-%d"

## -- Window style --
set -g status-position top
set -g status-style "bg=#{@thm_bg}"
set -g status-justify "absolute-centre"

setw -g pane-border-status top
setw -g pane-border-format ""
setw -g pane-active-border-style "bg=#{@thm_bg},fg=#{@thm_overlay_0}"
setw -g pane-border-style "bg=#{@thm_bg},fg=#{@thm_surface_0}"
setw -g pane-border-lines single

# Helper: dynamic icon by command
set -wg automatic-rename on
set -g automatic-rename-format "#{b:pane_current_command}"
# set -g automatic-rename-format "Window"

# set -g window-status-separator '|'
set -g window-status-format " #I: #{?#{==:#{pane_current_command},nvim},,\
#{?#{==:#{pane_current_command},zsh},,\
#{?#{==:#{pane_current_command},bash},,\
#{?#{==:#{pane_current_command},tmux},,\
#{?#{==:#{pane_current_command},git},,\
#{?#{==:#{pane_current_command},python},,\
#{?#{==:#{pane_current_command},ssh},󰣀,\
#{?#{==:#{pane_current_command},watch},󰥔,\
#{?#{==:#{pane_current_command},curl},,\
#{?#{==:#{pane_current_command},dig},,\
#{?#{==:#{pane_current_command},ss},󰩟,\
#{?#{==:#{pane_current_command},ansible},,\
#{?#{==:#{pane_current_command},tofu},,\
#{?#{==:#{pane_current_command},kubectl},⎈,\
}}}}}}}}}}}}}} #W "
set -g window-status-style "bg=#{@thm_bg},fg=#{@thm_rosewater}"
set -g window-status-last-style "bg=#{@thm_bg},fg=#{@thm_peach}"
set -g window-status-activity-style "bg=#{@thm_red},fg=#{@thm_bg}"
set -g window-status-bell-style "bg=#{@thm_red},fg=#{@thm_bg},bold"
set -gF window-status-separator "#[bg=#{@thm_bg},fg=#{@thm_overlay_0}]│"
set -g window-status-current-format " #I: #{?#{==:#{pane_current_command},nvim},,\
#{?#{==:#{pane_current_command},zsh},,\
#{?#{==:#{pane_current_command},bash},,\
#{?#{==:#{pane_current_command},tmux},,\
#{?#{==:#{pane_current_command},git},,\
#{?#{==:#{pane_current_command},python},,\
#{?#{==:#{pane_current_command},ssh},󰣀,\
#{?#{==:#{pane_current_command},watch},󰥔,\
#{?#{==:#{pane_current_command},curl},,\
#{?#{==:#{pane_current_command},dig},,\
#{?#{==:#{pane_current_command},ss},󰩟,\
#{?#{==:#{pane_current_command},ansible},,\
#{?#{==:#{pane_current_command},tofu},,\
#{?#{==:#{pane_current_command},kubectl},⎈,\
}}}}}}}}}}}}}} #W "

set -g window-status-current-style "bg=#{@thm_peach},fg=#{@thm_bg},bold"

# Configure Online
set -g @online_icon "ok"
set -g @offline_icon "nok"

## -- Sessions persistence --

# set -g @resurrect-capture-pane-contents 'on'
# set -g @continuum-restore 'on'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
